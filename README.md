# HH Bot

Telegram‑бот для поиска вакансий на hh.ru, персональных рекомендаций и генерации резюме через LLM.

## 🚀 Возможности

Бот помогает искать свежие вакансии, анализировать их и генерировать под них документы.

### 1. Профиль пользователя

- `/start` — создание профиля.
- Сохраняются:
  - Telegram ID
  - имя / username
  - город
  - желаемая должность
  - навыки
  - базовое резюме
- `/profile` — просмотр профиля и управление настройками.

### 2. Настройки поиска

Бот поддерживает гибкие фильтры поиска:

- должность
- город
- минимальная зарплата
- удалённость / метро
- свежесть вакансий (1–3 дня)
- тип занятости
- опыт работы
- тип компании

### 3. Поиск вакансий

- `/search` или просто текст в чат.
- Бот обращается к API hh.ru.
- Находит вакансии и сохраняет в базу.
- Формирует удобные карточки с:
  - названием
  - компанией
  - городом
  - зарплатой
  - ссылкой на hh.ru
- Под карточкой — кнопки:
  - «Сгенерировать резюме»
  - «Сгенерировать cover letter»
  - «Следующая вакансия»

### 4. Ежедневная рассылка вакансий

Раз в сутки бот:

- поднимает ваши фильтры,
- ищет новые вакансии,
- не шлёт дубликаты,
- отправляет 5–10 новых позиций.

### 5. Генерация документов через LLM

Поддержка любого OpenAI‑совместимого API:

- Base URL
- Model name
- API Key

Бот умеет:

- генерировать адаптированное резюме под вакансию,
- генерировать сопроводительное письмо.

## 🏗️ Техническая архитектура

### Технологии

- Python 3.11+
- aiogram 3
- PostgreSQL (Neon)
- async SQLAlchemy 2.0
- Alembic
- APScheduler
- httpx
- OpenAI‑compatible LLM API

### Модули

```
bot/
├── config.py
├── db/
│   ├── database.py
│   ├── models.py
│   └── repositories/
├── handlers/
├── services/
│   ├── hh_service.py
│   └── openai_service.py
└── utils/
    ├── logging.py
    └── scheduler.py
```

### Что хранится в базе

- пользователи
- фильтры поиска
- вакансии
- связи «пользователь ↔ вакансия»
- результаты поиска
- данные для генерации документов

## ⚙️ Настройка окружения

Создайте `.env`:

```
TG_BOT_API_KEY=...
DATABASE_URL=postgres://user:pass@host/dbname?sslmode=require
LLM_API_KEY=...
LLM_API_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini
LOG_LEVEL=DEBUG
```

## ▶️ Как запустить

```
uv sync
uv run alembic upgrade head
uv run python main.py
```

## 📝 Разработка

Рекомендации:

- Использовать Ruff и форматирование
- Проверить миграции Alembic
- Не хранить ключи в коде
- Логировать ошибки hh.ru и LLM

Проект готов к работе и дальнейшему расширению.
